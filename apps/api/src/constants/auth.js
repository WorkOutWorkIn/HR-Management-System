export const AUTH_TOKEN_TYPES = Object.freeze({
  ACCESS: 'access',
  REFRESH: 'refresh',
});

export const PASSWORD_TOKEN_PURPOSES = Object.freeze({
  ACCOUNT_SETUP: 'ACCOUNT_SETUP',
  PASSWORD_RESET: 'PASSWORD_RESET',
});

export const REFRESH_COOKIE_NAME = 'hrms_refresh_token';
